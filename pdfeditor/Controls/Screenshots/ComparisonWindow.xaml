<Window
	x:Class="pdfeditor.Controls.Screenshots.ComparisonWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:pdfeditor.Controls.Screenshots" xmlns:localprop="clr-namespace:pdfeditor.Properties"
	WindowStyle="None"
	ResizeMode="NoResize"
	ShowInTaskbar="False"
	Topmost="True"
	AllowsTransparency="True"
	Background="#00FFFFFF"
	Title="ComparisonWindow"
	Height="450"
	Width="800"
	MouseDoubleClick="Window_MouseDoubleClick">
	<Border
		x:Name="LayoutRoot"
		Opacity="1"
		Background="#FFE0E0E0"
		BorderBrush="#DD000000"
		BorderThickness="1">
		<FrameworkElement.Resources>
			<ResourceDictionary>
				<Style
					x:Key="HoverCommandButtonStyle"
					TargetType="{x:Type ButtonBase}">
					<Setter
						Property="Background"
						Value="#FFFFFFFF" />
					<Setter
						Property="BorderBrush"
						Value="#00FFFFFF" />
					<Setter
						Property="Foreground"
						Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
					<Setter
						Property="BorderThickness"
						Value="0" />
					<Setter
						Property="HorizontalContentAlignment"
						Value="Center" />
					<Setter
						Property="VerticalContentAlignment"
						Value="Center" />
					<Setter
						Property="VerticalAlignment"
						Value="Center" />
					<Setter
						Property="Padding"
						Value="0" />
					<Setter
						Property="Margin"
						Value="0,0,0,8" />
					<Setter
						Property="Width"
						Value="24" />
					<Setter
						Property="Height"
						Value="24" />
					<Setter
						Property="UIElement.Effect">
						<Setter.Value>
							<DropShadowEffect
								BlurRadius="6"
								ShadowDepth="0"
								Color="Black"
								Opacity="0.4" />
						</Setter.Value>
					</Setter>
					<Setter
						Property="Template">
						<Setter.Value>
							<ControlTemplate
								TargetType="{x:Type ButtonBase}">
								<Border
									x:Name="border"
									CornerRadius="4"
									BorderThickness="{TemplateBinding Control.BorderThickness}"
									Background="{TemplateBinding Control.Background}"
									BorderBrush="{TemplateBinding Control.BorderBrush}"
									SnapsToDevicePixels="True">
									<Grid>
										<Rectangle
											x:Name="BackgroundBorder"
											HorizontalAlignment="Stretch"
											VerticalAlignment="Stretch"
											Fill="#FF000000"
											Opacity="0"
											RadiusX="2"
											RadiusY="2" />
										<ContentPresenter
											x:Name="contentPresenter"
											RecognizesAccessKey="True"
											Margin="{TemplateBinding Control.Padding}"
											SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
											HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
											VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
											Focusable="False" />
									</Grid>
								</Border>
								<ControlTemplate.Triggers>
									<Trigger
										Property="UIElement.IsMouseOver"
										Value="True">
										<Setter
											TargetName="BackgroundBorder"
											Property="UIElement.Opacity"
											Value="0.04" />
									</Trigger>
									<Trigger
										Property="IsPressed"
										Value="True">
										<Setter
											TargetName="BackgroundBorder"
											Property="UIElement.Opacity"
											Value="0.12" />
									</Trigger>
									<Trigger
										Property="IsEnabled"
										Value="False">
										<Setter
											TargetName="border"
											Property="Border.Background"
											Value="{StaticResource DialogButton.Disabled.Background}" />
										<Setter
											TargetName="border"
											Property="Border.BorderBrush"
											Value="{StaticResource DialogButton.Disabled.Border}" />
										<Setter
											TargetName="contentPresenter"
											Property="TextElement.Foreground"
											Value="{StaticResource DialogButton.Disabled.Foreground}" />
									</Trigger>
								</ControlTemplate.Triggers>
							</ControlTemplate>
						</Setter.Value>
					</Setter>
				</Style>
			</ResourceDictionary>
		</FrameworkElement.Resources>
		<Grid>
			<StackPanel
				Orientation="Horizontal"
				ZIndex="2"
				HorizontalAlignment="Right"
				VerticalAlignment="Top">
				<Button
					x:Name="CopyButton"
					Width="24"
					Height="24"
					Style="{StaticResource HoverCommandButtonStyle}"
					FontFamily="Segoe MDL2 Assets"
					FontSize="16"
					Content=""
					Margin="4"
					Padding="2,0"
					Click="CopyButton_Click">
					<FrameworkElement.ToolTip>
						<ToolTip
							Content="{x:Static localprop:Resources.WinScreenshotToolbarCopyContent}" />
					</FrameworkElement.ToolTip>
				</Button>
				<Button
					x:Name="ScaleButton"
					Width="24"
					Height="24"
					Style="{StaticResource HoverCommandButtonStyle}"
					FontFamily="Segoe MDL2 Assets"
					FontSize="16"
					Content=""
					Margin="4"
					Padding="2,0"
					Click="ScaleButton_Click">
					<FrameworkElement.ToolTip>
						<ToolTip
							Content="{x:Static localprop:Resources.WinScreenshotToolbarZoomInContent}" />
					</FrameworkElement.ToolTip>
				</Button>
				<Button
					x:Name="DeleteButton"
					Width="24"
					Height="24"
					Style="{StaticResource HoverCommandButtonStyle}"
					FontFamily="Segoe MDL2 Assets"
					FontSize="16"
					Content=""
					Margin="4"
					Padding="2,0"
					Click="DeleteButton_Click">
					<FrameworkElement.ToolTip>
						<ToolTip
							Content="{x:Static localprop:Resources.WinScreenshotToolbarDeleteContent}" />
					</FrameworkElement.ToolTip>
				</Button>
			</StackPanel>
			<Image
				x:Name="ContentImage"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Stretch"
				Stretch="Uniform" />
		</Grid>
	</Border>
</Window>
