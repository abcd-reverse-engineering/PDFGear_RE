<Window
	x:Class="pdfeditor.Controls.FlattenWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:pdfeditor.Controls" xmlns:localprop="clr-namespace:pdfeditor.Properties"
	ResizeMode="NoResize"
	Background="{DynamicResource WindowsBackground}"
	Foreground="{DynamicResource TextBrushWhiteAndBlack}"
	Title="PDFgear"
	SizeToContent="Height"
	Width="540">
	<FrameworkElement.Resources>
		<ResourceDictionary>
			<Style
				x:Key="TextBoxStyle1"
				TargetType="{x:Type TextBox}">
				<Setter
					Property="Template">
					<Setter.Value>
						<ControlTemplate
							TargetType="{x:Type TextBox}">
							<Border
								x:Name="border"
								Background="{TemplateBinding Control.Background}"
								BorderBrush="{TemplateBinding Control.BorderBrush}"
								BorderThickness="{TemplateBinding Control.BorderThickness}"
								SnapsToDevicePixels="True">
								<Grid>
									<ScrollViewer
										x:Name="PART_ContentHost"
										Focusable="False"
										HorizontalScrollBarVisibility="Hidden"
										VerticalScrollBarVisibility="Hidden"
										Visibility="Collapsed" />
									<TextBlock
										x:Name="Chrome"
										Margin="2.38,0"
										HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
										VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
										Text="{TemplateBinding TextBox.Text}"
										TextTrimming="CharacterEllipsis" />
								</Grid>
							</Border>
							<ControlTemplate.Triggers>
								<Trigger
									Property="UIElement.IsFocused"
									Value="True">
									<Setter
										TargetName="Chrome"
										Property="Visibility"
										Value="Collapsed" />
									<Setter
										TargetName="PART_ContentHost"
										Property="Visibility"
										Value="Visible" />
								</Trigger>
								<Trigger
									Property="IsEnabled"
									Value="False">
									<Setter
										Value="{DynamicResource MainViewFontSizeBorder}"
										Property="Foreground" />
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
		</ResourceDictionary>
	</FrameworkElement.Resources>
	<Grid
		Margin="20 0">
		<Grid.RowDefinitions>
			<RowDefinition
				Height="40" />
			<RowDefinition
				Height="Auto" />
			<RowDefinition
				Height="Auto" />
			<RowDefinition
				Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock
			Text="{x:Static localprop:Resources.FlattenWinTitle}"
			FontSize="14"
			FontWeight="Bold"
			HorizontalAlignment="Left"
			VerticalAlignment="Bottom" />
		<Grid
			Row="1"
			Background="{DynamicResource BatchPrintSiderbarBackground}"
			Margin="0 5">
			<Grid.RowDefinitions>
				<RowDefinition
					Height="Auto" />
				<RowDefinition
					Height="Auto" />
				<RowDefinition
					Height="Auto" />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition
					Width="30" />
				<ColumnDefinition
					Width="*" />
			</Grid.ColumnDefinitions>
			<TextBlock
				FontFamily="Segoe MDL2 Assets"
				Text=""
				HorizontalAlignment="Right"
				VerticalAlignment="Top"
				Margin="0 7 5 0" />
			<TextBlock
				Text="{x:Static localprop:Resources.FlattenWinContent1}"
				Grid.Column="1"
				TextWrapping="Wrap"
				Margin="0 5"
				LineHeight="18" />
			<TextBlock
				FontFamily="Segoe MDL2 Assets"
				Text=""
				Grid.Row="1"
				HorizontalAlignment="Right"
				VerticalAlignment="Top"
				Margin="0 7 5 0" />
			<TextBlock
				Text="{x:Static localprop:Resources.FlattenWinContent2}"
				Grid.Row="1"
				Grid.Column="1"
				Margin="0 5"
				LineHeight="18"
				TextWrapping="Wrap" />
			<TextBlock
				FontFamily="Segoe MDL2 Assets"
				Text=""
				Grid.Row="2"
				HorizontalAlignment="Right"
				VerticalAlignment="Top"
				Margin="0 7 5 0" />
			<TextBlock
				Text="{x:Static localprop:Resources.FlattenWinContent3}"
				Grid.Row="2"
				Grid.Column="1"
				Margin="0 5"
				LineHeight="18"
				TextWrapping="Wrap" />
		</Grid>
		<Grid
			Row="2">
			<Grid.RowDefinitions>
				<RowDefinition
					Height="0" />
				<RowDefinition />
				<RowDefinition />
				<RowDefinition
					Height="1.2*" />
				<RowDefinition />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition
					MaxWidth="80" />
				<ColumnDefinition
					Width="120" />
				<ColumnDefinition
					Width="*" />
			</Grid.ColumnDefinitions>
			<TextBlock
				Text="{x:Static localprop:Resources.WinPageSplitBtnPagerangeContent}"
				FontSize="12"
				HorizontalAlignment="Left"
				TextWrapping="Wrap"
				Grid.Row="1"
				Foreground="{DynamicResource MainViewIndeterminate}"
				VerticalAlignment="Center" />
			<RadioButton
				x:Name="AllPageBtn"
				Content="{x:Static localprop:Resources.SelectPageAllPagesItem}"
				Grid.Row="1"
				Grid.Column="1"
				Style="{StaticResource DefaultRadioButtonStyle}"
				Grid.ColumnSpan="2"
				GroupName="PageRange"
				HorizontalAlignment="Left"
				VerticalAlignment="Center"
				Margin="20 10 0 5"
				IsChecked="True" />
			<Grid
				Row="2"
				Column="1"
				ColumnSpan="2"
				Margin="0 10">
				<Grid.ColumnDefinitions>
					<ColumnDefinition
						Width="Auto" />
					<ColumnDefinition
						Width="Auto" />
				</Grid.ColumnDefinitions>
				<RadioButton
					x:Name="SelectedPageBtn"
					GroupName="PageRange"
					Grid.Column="0"
					Content="{x:Static localprop:Resources.PageWinSelectPages}"
					Style="{StaticResource DefaultRadioButtonStyle}"
					HorizontalAlignment="Left"
					VerticalAlignment="Center"
					Margin="20 0 0 0" />
				<TextBlock
					x:Name="Range"
					Grid.Column="1"
					Foreground="{DynamicResource MainViewIndeterminate}"
					Margin="7 0 0 0"
					Text=""
					VerticalAlignment="Center"
					TextTrimming="WordEllipsis" />
			</Grid>
			<Grid
				Row="3"
				RowSpan="2"
				ColumnSpan="2"
				Column="1"
				Margin="0 0 0 0">
				<Grid.RowDefinitions>
					<RowDefinition
						Height="25" />
					<RowDefinition
						Height="20" />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition
						Width="Auto" />
					<ColumnDefinition
						Width="*" />
				</Grid.ColumnDefinitions>
				<RadioButton
					x:Name="CustomBtn"
					Content="{x:Static localprop:Resources.PageWinCustomPages}"
					Width="Auto"
					HorizontalAlignment="Left"
					Grid.Row="0"
					Style="{StaticResource DefaultRadioButtonStyle}"
					GroupName="PageRange"
					Margin="20 0 0 0"
					VerticalAlignment="Center" />
				<TextBox
					x:Name="CustomTextBox"
					Grid.Row="0"
					Grid.Column="1"
					Height="22"
					Margin="18 0 0 0"
					HorizontalAlignment="Stretch"
					Style="{StaticResource DefaultTextBoxStyle}"
					VerticalContentAlignment="Center"
					BorderBrush="#FFD2D2D2"
					IsEnabled="{Binding ElementName=CustomBtn, Path=IsChecked}"
					LostFocus="CustomTextBox_LostFocus" />
				<TextBlock
					x:Name="CustomTextBolck"
					Grid.Column="1"
					Grid.Row="1"
					Margin="18 0"
					VerticalAlignment="Center"
					Text="eg. 1,8,9-12" />
				<FrameworkElement.Style>
					<Style
						TargetType="{x:Type Grid}">
						<Style.Triggers>
							<Trigger
								Property="IsEnabled"
								Value="False">
								<Setter
									Property="TextBlock.Foreground"
									Value="#D2D2D2" />
							</Trigger>
						</Style.Triggers>
					</Style>
				</FrameworkElement.Style>
			</Grid>
		</Grid>
		<Border
			Grid.Row="3"
			BorderThickness="0 1 0 0"
			BorderBrush="#D2D2D2"
			Margin="0 15 0 0">
			<Grid
				x:Name="ButtonContainer"
				HorizontalAlignment="Right">
				<FrameworkElement.Resources>
					<ResourceDictionary>
						<SolidColorBrush
							x:Key="PrimaryDialogButton.Static.Background"
							Color="#EF5B52" />
						<SolidColorBrush
							x:Key="PrimaryDialogButton.Static.Border"
							Color="#FFEF5B52" />
						<SolidColorBrush
							x:Key="PrimaryDialogButton.Static.Foreground"
							Color="#FFFFFFFF" />
						<SolidColorBrush
							x:Key="DialogButton.Static.Background"
							Color="#FFFFFFFF" />
						<SolidColorBrush
							x:Key="DialogButton.Static.Border"
							Color="#FFBBBBBB" />
						<SolidColorBrush
							x:Key="DialogButton.Static.Foreground"
							Color="#FF000000" />
						<SolidColorBrush
							x:Key="DialogButton.Disabled.Background"
							Color="#FFF4F4F4" />
						<SolidColorBrush
							x:Key="DialogButton.Disabled.Border"
							Color="#FFF4F4F4" />
						<SolidColorBrush
							x:Key="DialogButton.Disabled.Foreground"
							Color="#FF838383" />
						<Style
							x:Key="DialogButtonFocusVisual">
							<Setter
								Property="Control.Template">
								<Setter.Value>
									<ControlTemplate>
										<Rectangle
											Margin="-1"
											SnapsToDevicePixels="False"
											Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
											StrokeThickness="1"
											RadiusX="2"
											RadiusY="2"
											Opacity="0.6" />
									</ControlTemplate>
								</Setter.Value>
							</Setter>
						</Style>
						<Style
							x:Key="DialogButtonStyle"
							TargetType="{x:Type ButtonBase}">
							<Setter
								Property="Background"
								Value="{StaticResource DialogButton.Static.Background}" />
							<Setter
								Property="BorderBrush"
								Value="{StaticResource DialogButton.Static.Border}" />
							<Setter
								Property="Foreground"
								Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
							<Setter
								Property="BorderThickness"
								Value="1" />
							<Setter
								Property="HorizontalContentAlignment"
								Value="Center" />
							<Setter
								Property="VerticalContentAlignment"
								Value="Center" />
							<Setter
								Property="VerticalAlignment"
								Value="Center" />
							<Setter
								Property="Padding"
								Value="8" />
							<Setter
								Property="MinWidth"
								Value="80" />
							<Setter
								Property="MinHeight"
								Value="32" />
							<Setter
								Property="FrameworkElement.FocusVisualStyle"
								Value="{StaticResource DialogButtonFocusVisual}" />
							<Setter
								Property="Template">
								<Setter.Value>
									<ControlTemplate
										TargetType="{x:Type ButtonBase}">
										<Border
											x:Name="border"
											CornerRadius="2"
											BorderThickness="{TemplateBinding Control.BorderThickness}"
											Background="{TemplateBinding Control.Background}"
											BorderBrush="{TemplateBinding Control.BorderBrush}"
											SnapsToDevicePixels="True">
											<Grid>
												<Rectangle
													x:Name="BackgroundBorder"
													HorizontalAlignment="Stretch"
													VerticalAlignment="Stretch"
													Fill="#FF000000"
													Opacity="0"
													SnapsToDevicePixels="False"
													RadiusX="2"
													RadiusY="2" />
												<ContentPresenter
													x:Name="contentPresenter"
													RecognizesAccessKey="True"
													Margin="{TemplateBinding Control.Padding}"
													SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
													HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
													VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
													Focusable="False" />
											</Grid>
										</Border>
										<ControlTemplate.Triggers>
											<Trigger
												Property="UIElement.IsMouseOver"
												Value="True">
												<Setter
													TargetName="BackgroundBorder"
													Property="UIElement.Opacity"
													Value="0.04" />
											</Trigger>
											<Trigger
												Property="IsPressed"
												Value="True">
												<Setter
													TargetName="BackgroundBorder"
													Property="UIElement.Opacity"
													Value="0.12" />
											</Trigger>
											<Trigger
												Property="IsEnabled"
												Value="False">
												<Setter
													TargetName="border"
													Property="Border.Background"
													Value="{StaticResource DialogButton.Disabled.Background}" />
												<Setter
													TargetName="border"
													Property="Border.BorderBrush"
													Value="{StaticResource DialogButton.Disabled.Border}" />
												<Setter
													TargetName="contentPresenter"
													Property="TextElement.Foreground"
													Value="{StaticResource DialogButton.Disabled.Foreground}" />
											</Trigger>
											<MultiTrigger>
												<MultiTrigger.Conditions>
													<Condition
														Property="Button.IsDefault"
														Value="True" />
												</MultiTrigger.Conditions>
												<Setter
													Property="Background"
													Value="{StaticResource PrimaryDialogButton.Static.Background}" />
												<Setter
													Property="BorderBrush"
													Value="{StaticResource PrimaryDialogButton.Static.Border}" />
												<Setter
													Property="Foreground"
													Value="{StaticResource PrimaryDialogButton.Static.Foreground}" />
											</MultiTrigger>
										</ControlTemplate.Triggers>
									</ControlTemplate>
								</Setter.Value>
							</Setter>
						</Style>
					</ResourceDictionary>
				</FrameworkElement.Resources>
				<Grid.ColumnDefinitions>
					<ColumnDefinition
						Width="Auto" />
					<ColumnDefinition
						Width="Auto" />
					<ColumnDefinition
						Width="Auto" />
				</Grid.ColumnDefinitions>
				<Button
					x:Name="PrimaryButton"
					Grid.Column="2"
					Height="32"
					FontSize="12"
					Style="{StaticResource PrimaryDialogButtonStyle}"
					MinWidth="110"
					Content="{x:Static localprop:Resources.WinBtnOkContent}"
					Margin="10 20 0 20"
					Click="PrimaryButton_Click" />
			</Grid>
		</Border>
	</Grid>
</Window>
