<local:TranslatePopupWindow
	x:Class="pdfeditor.Controls.Translation.Popups.TranslationWin" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:pdfeditor.Controls.Translation.Popups" xmlns:viewmodels="clr-namespace:pdfeditor.ViewModels" xmlns:commonLibControl="clr-namespace:CommonLib.Controls;assembly=CommonLib" xmlns:cv="clr-namespace:pdfeditor.Utils.Converters"
	MouseMove="Window_MouseMove"
	MouseLeave="TranslatePopupWindow_MouseLeave"
	Title="Translate"
	Height="230"
	Width="500">
	<FrameworkElement.Resources>
		<ResourceDictionary>
			<cv:BooleanToVisibilityConverter
				x:Key="BooleanToVisibilityConverter" />
			<cv:BooleanToVisibilityReverseConverter
				x:Key="BooleanToVisibilityReverseConverter" />
			<Style
				x:Key="Btnstyle"
				TargetType="{x:Type ButtonBase}">
				<Setter
					Property="Template">
					<Setter.Value>
						<ControlTemplate
							TargetType="{x:Type ButtonBase}">
							<Border
								x:Name="bborder"
								BorderBrush="{TemplateBinding Control.BorderBrush}"
								CornerRadius="4"
								BorderThickness="{TemplateBinding Control.BorderThickness}"
								Background="{TemplateBinding Control.Background}"
								SnapsToDevicePixels="True">
								<ContentPresenter
									x:Name="contentPresenter"
									Focusable="False"
									HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
									Margin="{TemplateBinding Control.Padding}"
									RecognizesAccessKey="True"
									SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
									VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
							</Border>
							<ControlTemplate.Triggers>
								<Trigger
									Property="UIElement.IsMouseOver"
									Value="True">
									<Setter
										TargetName="bborder"
										Property="Border.Background"
										Value="#FFF0F0" />
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
		</ResourceDictionary>
	</FrameworkElement.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition
				Height="Auto" />
			<RowDefinition
				Height="auto" />
			<RowDefinition
				Height="40" />
		</Grid.RowDefinitions>
		<Rectangle
			Stroke="#D2D2D2"
			Fill="#D2D2D2"
			StrokeThickness="1"
			Height="1"
			Width="auto"
			Margin="20 2" />
		<ScrollViewer
			Grid.Row="1"
			VerticalScrollBarVisibility="Auto"
			HorizontalScrollBarVisibility="Disabled"
			Height="160">
			<Grid>
				<TextBox
					x:Name="Translation"
					Margin="10"
					TextWrapping="Wrap"
					IsReadOnly="True"
					BorderThickness="0"
					Visibility="{Binding Translating, Converter='{StaticResource BooleanToVisibilityReverseConverter}'}"
					Text="{Binding Translations}" />
				<commonLibControl:ProgressRing
					Width="16"
					Height="16"
					IsActive="True"
					HorizontalAlignment="Left"
					VerticalAlignment="Top"
					Foreground="#EF5B52"
					Margin="12,5,0,0"
					Visibility="{Binding Translating, Converter='{StaticResource BooleanToVisibilityConverter}'}" />
			</Grid>
		</ScrollViewer>
		<Button
			Grid.Row="2"
			Style="{StaticResource Btnstyle}"
			Background="#00FFFFFF"
			VerticalAlignment="Center"
			HorizontalAlignment="Right"
			BorderThickness="0"
			Margin="0 0 17 20"
			Command="{Binding OpenTranslatePanelCommand}">
			<TextBlock
				Text="Open Translation Window &gt;"
				TextWrapping="Wrap"
				VerticalAlignment="Bottom"
				HorizontalAlignment="Center"
				Foreground="#EF5B52"
				TextDecorations="Underline" />
		</Button>
	</Grid>
</local:TranslatePopupWindow>
